# Sentinel
AI-powered incident investigation copilot built in Java + Spring Boot — uses RAG (Gemini embeddings + cosine similarity) to ground diagnoses in real past incidents instead of guessing blind.
