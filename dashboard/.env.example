# Sentinel Spring Boot API. Never put Razorpay credentials in dashboard variables.
NEXT_PUBLIC_SENTINEL_API_URL=http://localhost:8080

# Optional visual-review fixture mode. Keep false for the real five-minute demo.
NEXT_PUBLIC_USE_FIXTURES=false
